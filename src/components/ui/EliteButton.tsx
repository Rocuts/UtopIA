/**
 * @deprecated Use `Button` with `variant="elite"` / `variant="wine"` from
 * `@/components/ui/Button` instead. This module is a backward-compatible
 * re-export — unified into Button during Sprint 2 design-system consolidation.
 */
export {
  Button as EliteButton,
  type ButtonProps as EliteButtonProps,
  type ButtonVariant as EliteButtonVariant,
  type ButtonSize as EliteButtonSize,
} from './Button';
