/**
 * @deprecated Use `Card` with `variant="glass" | "solid" | "bordered"` from
 * `@/components/ui/Card` instead. This module is a backward-compatible
 * re-export — unified into Card during Sprint 2 design-system consolidation.
 */
export {
  Card as EliteCard,
  type CardProps as EliteCardProps,
  type CardVariant as EliteCardVariant,
  type CardHover as EliteCardHover,
  type CardPadding as EliteCardPadding,
} from './Card';
